// terminal 中 输入node 直接进入node环境 
// terminal 运行方式 node filename 

/**
 * 求两个数之和
 * @param  {[type]}
 * @param  {[type]}
 * @return {[type]}
 */
function sum(x, y) {
	return x + y;
}

console.log(sum(4, 5))